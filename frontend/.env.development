# Local dev: leave VITE_API_URL empty — Vite proxies /player, /admin/*, /wheel → :3000
# To debug against a remote API (Render) while running Vite:
# VITE_USE_REMOTE_API=true
# VITE_API_URL=https://spinywheely-api.onrender.com
